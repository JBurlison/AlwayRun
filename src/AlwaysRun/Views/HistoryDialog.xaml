<Window x:Class="AlwaysRun.Views.HistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AlwaysRun.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" 
        Height="500" Width="820"
        MinHeight="350" MinWidth="650"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        d:DataContext="{d:DesignInstance Type=vm:HistoryViewModel}">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
    </Window.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Event List -->
        <DataGrid Grid.Row="0"
                  ItemsSource="{Binding Events}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F8F8F8"
                  VerticalScrollBarVisibility="Auto"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="140"/>
                <DataGridTextColumn Header="Event" Binding="{Binding EventType}" Width="110">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding EventType}" Value="Crashed">
                                    <Setter Property="Foreground" Value="Red"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding EventType}" Value="Restarted">
                                    <Setter Property="Foreground" Value="Green"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding EventType}" Value="Restart Failed">
                                    <Setter Property="Foreground" Value="OrangeRed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding EventType}" Value="Started">
                                    <Setter Property="Foreground" Value="DodgerBlue"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding EventType}" Value="Stopped">
                                    <Setter Property="Foreground" Value="Gray"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding EventType}" Value="Paused">
                                    <Setter Property="Foreground" Value="Orange"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding EventType}" Value="Resumed">
                                    <Setter Property="Foreground" Value="DodgerBlue"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Exit Code" Binding="{Binding ExitCode}" Width="80"/>
                <DataGridTextColumn Header="Uptime" Binding="{Binding Uptime}" Width="100"/>
                <DataGridTextColumn Header="Attempt" Binding="{Binding Attempt}" Width="70"/>
                <DataGridTextColumn Header="Error" Binding="{Binding Error}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Summary -->
        <TextBlock Grid.Row="1"
                   Text="{Binding Summary}"
                   Margin="0,0,0,10"
                   Foreground="Gray"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Clear History"
                    Command="{Binding ClearHistoryCommand}"
                    Padding="15,5"
                    MinWidth="100"
                    Margin="0,0,10,0"/>
            <Button Content="Close"
                    Command="{Binding CloseCommand}"
                    IsCancel="True"
                    Padding="15,5"
                    MinWidth="80"/>
        </StackPanel>
        
        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3"
              Background="#80FFFFFF"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}">
            <ProgressBar IsIndeterminate="True" Width="200" Height="20"
                         HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
